############################################
#        THIS FILE IS AUTOGENERATED 
#
# ANY MODIFICATIONS TO THIS FILE WILL BE 
# OVERWRITTEN BY THE CI/CD PIPELINE 
#
# Generation Timestamp: 2020-09-14 12:05:06.047707
############################################
#
# Default values
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
#
#

replicaCount: 1

deploymentNamespace: tssc-team-ref-app-quarkus-backend-master-dev

service: ref-app-quarkus-backend
image:
  repository: quay-quay-enterprise.apps.tssc.rht-set.com/tssc-team/ref-app-quarkus-backend
  tag: 1.0.2
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetport: 8080

probe: 
  readypath: /fruits
  livepath:  /fruits

route:
  enabled: True
  annotations: {
  }
  path: /
  hostname: backend.tssc-team-ref-app-quarkus-backend-master-dev.dev.apps.tssc.rht-set.com

  tls: []
  #  - secretName: chart-example-tls
  #    hosts:   #      - chart-example.local

resources: 
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:   #  cpu: 100m
  #  memory: 128Mi
  # requests:   #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {
} 

tolerations: []

affinity: {
}
